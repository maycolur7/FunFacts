<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/relativLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.whatscode.funfacts.MainActivity"
    android:background="@color/colorPrimary">

    <TextView
        android:id="@+id/first_text"
        android:layout_marginTop="@dimen/facts_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/first_text"
        android:textColor="@android:color/white"
        android:textSize="@dimen/facts_text"
        android:paddingLeft="@dimen/text_padding_left"/>

    <TextView
        android:id="@+id/second_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/second_text"
        android:textColor="@android:color/white"
        android:textSize="@dimen/facts_text"
        android:paddingLeft="@dimen/text_padding_left"
        android:layout_centerVertical="true" />

    <Button
        android:id="@+id/showFactBotton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerInParent="true"
        android:text="@string/third_text"
        android:textColor="@color/colorPrimary"
        android:layout_marginBottom="@dimen/facts_text"
        android:layout_marginLeft="@dimen/facts_text"
        android:background="@android:color/white"/>


</RelativeLayout>
